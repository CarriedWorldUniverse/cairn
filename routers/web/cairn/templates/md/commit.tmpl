# Commit {{.Commit.SHA}}

**Author:** {{.Commit.AuthorName}} <{{.Commit.AuthorEmail}}>{{if .IsAgent}}
**Agent:** {{.AgentBadge}} (under {{if .OwnerUsername}}{{.OwnerUsername}}{{else if .AuthorOwner}}{{.AuthorOwner}}{{else}}unknown{{end}}){{end}}
**Date:** {{.Commit.Time.UTC.Format "2006-01-02T15:04:05Z"}}
**Signed:** {{if and .Commit.Signed .Commit.Verified}}verified{{else if .Commit.Signed}}yes (unverified){{else}}no{{end}}
**Repo:** {{.Repo.Owner}}/{{.Repo.Name}}

## Message

{{.Commit.Subject}}
{{if .Commit.Body}}
{{.Commit.Body}}
{{end}}
{{if .Commit.Diff}}## Diff

```diff
{{.Commit.Diff}}
```
{{end}}
