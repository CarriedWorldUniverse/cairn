apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cairn-data
  namespace: cwb
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
