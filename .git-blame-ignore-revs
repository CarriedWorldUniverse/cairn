# Revisions ignored by `git blame --ignore-revs-file=.git-blame-ignore-revs`.
#
# Add commits here that are large, mechanical, and uninteresting for blame
# purposes (mass rewrites, code formatting changes, etc). Configure git to
# read this file once per clone:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Each entry is a commit hash followed by a comment describing what the
# commit did. New entries go at the bottom.

# TODO: append the lock-day-one commit hash here after this PR merges.
# That commit rewrites every Go import path from forgejo.org/... to
# github.com/CarriedWorldUniverse/cairn/... — mechanical, not interesting
# for blame.
